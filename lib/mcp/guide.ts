// Тексти для агента: instructions сервера (отримує кожен клієнт при підключенні)
// і довідник полів (ресурс inventarium://guide/fields).
//
// Пояснення полів узгоджені з підказками на сторінці /record/[id]: якщо агент
// і сайт по-різному розтлумачать «кількість сторінок», дослідник не розбереться,
// кому вірити. Назви полів беремо з labels.ts — ті самі, що в самих відповідях.

import { INVENTORY_TYPES } from '../inventoryType';
import { L } from './labels';
import { SITE_URL } from './links';

export const SERVER_INSTRUCTIONS = `
Інвентаріум (${SITE_URL}) — волонтерський публічний реєстр інвентарних описів маєтків, що стосуються українських земель. Інвентарі фіксували землю, будівлі, населення й повинності селян — це джерело для істориків, краєзнавців і генеалогів.

Як влаштовано реєстр:
- Запис = інвентар одного населеного пункту в одній архівній справі за один рік. Одна справа (шифр) часто містить інвентарі кількох населених пунктів і років; один населений пункт трапляється в кількох справах.
- Інструменти повертають лише записи, підтверджені модераторами.
- Населений пункт має два описи: сучасний (країна, область, район, громада, назва — за довідником) і давній (воєводство/губернія, повіт, ключ/староство, назва — як у документі). Давня назва може відрізнятися від сучасної.
- «${L.markType}: регіон» означає, що запис прив'язано до околиць населеного пункту: у справі можуть бути й сусідні села.
- ${L.caseSignature}: для українського архіву «АРХІВ фонд-опис-справа» («ЦДІАК 1-2-3»), для іноземного — як в архіві («AGAD ASK 1/7/0/9/4»). «${L.additionalSignatures}» — та сама справа в інших архівах.
- «${L.pagesCount}» — за архівним описом, а не кількість сканів.
- Типи документів: ${INVENTORY_TYPES.join(', ')}. Детально — ресурс inventarium://guide/fields.

Як шукати:
- Населений пункт предків: find_settlement (уточнити, який саме з однойменних) → get_settlement_inventories за code → historical_divisions → search_inventories за давньою назвою → search_keys.
- Справа за шифром: get_case. Довільний текст чи фільтри: search_inventories.

Як відповідати:
- Мовою користувача. Кожен згаданий запис, справу чи населений пункт супроводжуй посиланням із відповіді інструмента; нічого не домислюй понад дані.
- Відповіді інструментів уже підписані українською — використовуй ці назви полів, а не вигадуй технічні.
- Якщо нічого не знайдено, так і скажи. Запропонуй перевірити давню назву, неідентифіковані справи (list_unidentified) або додати відомий користувачеві інвентар на сайті: ${SITE_URL}/add_inventory.
`.trim();

export const FIELDS_GUIDE = `
# Інвентаріум: поля запису

## Документ
- **${L.inventoryYear}** — рік, коли було складено інвентар по цьому населеному пункту.
- **${L.inventoryType}**:
  - Інвентар — опис маєтку;
  - Люстрація — ревізія королівських маєтків;
  - Фасія — податкова декларація;
  - Урбар — опис повинностей підданих;
  - Дефтер — османський податковий реєстр.
- **${L.inventoryStartPage}** — номер сторінки справи, з якої починається інвентар цього населеного пункту.
- **${L.markType}** — «місце»: інвентар точно стосується цього населеного пункту; «регіон»: запис прив'язано до околиць (у документі названо лише ключ чи повіт), тож у справі можуть бути й сусідні населені пункти.

## ${L.caseGroup}
- **${L.caseSignature}** — шифр, за яким справу можна замовити в архіві. Український архів: «АРХІВ фонд-опис-справа» (наприклад, «ЦДІАК 1-2-3»); іноземний — у форматі того архіву («AGAD ASK 1/7/0/9/4»).
- **${L.ukrainianArchive}**, **${L.archive}**, **${L.fonds}**, **${L.series}**, **${L.record}** — складові шифру українського архіву.
- **${L.additionalSignatures}** — шифри тієї самої справи в інших архівах.
- **${L.caseTitle}** — назва справи за архівним описом.
- **${L.caseDates}** — можуть не збігатися з роком складання інвентаря.
- **${L.pagesCount}** — загальна кількість сторінок справи за архівним описом. НЕ дорівнює кількості сканів.
- **${L.scansUrl}** — якщо скани є у відкритому доступі; у списках — «${L.scans}: є / немає».

## Населений пункт
- **${L.settlementCurrent}** — адмінподіл на час додавання запису: країна, регіон (область, воєводство, край), район (повіт, округ), громада (ґміна, сільрада), тип і назва.
- **${L.settlementHistorical}** — адмінподіл на час складання інвентаря: воєводство (губернія), повіт, ключ (староство), тип і назва — як у документі.
- «Немає» в довіднику означає, що рівня не існує (громади в Польщі, район і громада для м. Києва); такі рівні у відповідях пропущено.

## Інше
- **Ключ** — історичний комплекс маєтків: центр і населені пункти, що до нього входили, з джерелом.
- **Неідентифікована справа** — справа з інвентарями, для яких ще не встановлено населений пункт.
- **${L.cobook}** — проєкт колективного транскрибування інвентаря на cobook.today.
`.trim();
